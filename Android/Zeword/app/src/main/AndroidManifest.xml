<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ZewordApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zeword"
        tools:targetApi="31">
        <activity
            android:name=".frontend.reader.ReaderActivity"
            android:exported="false"
            android:label="@string/title_activity_reader"
            android:theme="@style/Theme.Zeword.NoActionBar" />
        <activity
            android:name=".frontend.home.HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.Zeword" />
        <activity
            android:name=".frontend.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Zeword">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>